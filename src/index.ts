export interface options {}

/**
 * in ES module
 * const waitForScrollCotent = require('waitforscrollcontent')
 * 
 * in Common JS
 * import { waitForScrollContent } from 'waitforscrollcontent'
 */
export function waitForScrollContent() {
  console.log('hello')
}